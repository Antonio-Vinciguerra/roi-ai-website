# ROI AI — Website Prototype

A lightweight, dependency-free interactive prototype for ROI AI.

## Run locally

Open `index.html` in a browser, or serve the folder with any static-file server.

## Notes

- Responsive desktop and mobile layouts
- No backend, analytics, fabricated metrics, or client logos
- The contact link intentionally uses a placeholder `.example` email and should be replaced before publishing
